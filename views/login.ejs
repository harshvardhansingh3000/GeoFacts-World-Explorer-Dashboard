<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - GeoFacts</title>
  <link rel="stylesheet" href="/css/style.css" />
  <script>
    function togglePassword() {
      const pwd = document.getElementById('password');
      const icon = document.getElementById('togglePwdIcon');
      if (pwd.type === 'password') {
        pwd.type = 'text';
        icon.textContent = '🙈';
      } else {
        pwd.type = 'password';
        icon.textContent = '👁️';
      }
    }
  </script>
</head>
<body class="bg-gradient-to-br from-blue-100 via-blue-300 to-blue-500 dark:from-gray-900 dark:via-gray-800 dark:to-gray-700 min-h-screen">
  <%- include("partials/navbar.ejs") %>
  <div class="flex items-center justify-center min-h-[80vh]">
    <div class="bg-white dark:bg-gray-900 p-8 rounded-lg shadow-lg w-full max-w-md">
      <h1 class="text-2xl font-bold text-blue-700 mb-6">Login to GeoFacts</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-100 text-red-700 px-4 py-2 rounded mb-4"><%= error %></div>
      <% } %>
      <form action="/login" method="POST" class="space-y-4">
        <div>
          <label class="block text-blue-900 dark:text-blue-200 font-semibold mb-1">Email</label>
          <input type="email" name="email" class="w-full dark:text-amber-50 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400" required />
        </div>
        <div>
          <label class="block text-blue-900 dark:text-blue-200 font-semibold mb-1">Password</label>
          <div class="relative">
            <input id="password" type="password" name="password" class="w-full dark:text-amber-50 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400 pr-10" required />
            <button type="button" onclick="togglePassword()" tabindex="-1" class="absolute inset-y-0 right-0 flex items-center px-2 text-gray-500 dark:text-gray-300 focus:outline-none">
              <span id="togglePwdIcon" class="text-xl">&#128065;</span>
            </button>
          </div>
        </div>
        <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded shadow font-semibold transition w-full">Login</button>
      </form>
      <div class="mt-4 text-center">
        <span class="text-gray-700 dark:text-gray-300">Don't have an account?</span>
        <a href="/register" class="text-blue-700 hover:underline font-semibold">Register</a>
      </div>
    </div>
  </div>
</body>
</html> 