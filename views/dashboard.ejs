<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GeoFacts Dashboard</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="bg-gradient-to-br from-blue-100 via-blue-300 to-blue-500 dark:from-gray-900 dark:via-gray-800 dark:to-gray-700 min-h-screen flex items-center justify-center">
  <div class="bg-white dark:bg-gray-900 rounded-2xl shadow-2xl p-10 max-w-lg w-full text-center">
    <h1 class="text-4xl font-extrabold mb-4 text-blue-700 dark:text-blue-300 tracking-tight">🌍 GeoFacts</h1>
    <% if (user) { %>
      <h2 class="text-2xl font-semibold mb-6 text-gray-800 dark:text-gray-100">Welcome, <span class="text-blue-600 dark:text-blue-400"><%= user.name %></span>!</h2>
      <p class="mb-8 text-gray-600 dark:text-gray-300">Ready to explore the world? Dive into the interactive map and start your journey!</p>
      <div class="flex flex-col gap-4">
        <a href="/map" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg transition">Get Started</a>
        <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white font-bold py-3 rounded-lg transition">Logout</a>
      </div>
    <% } else { %>
      <h2 class="text-2xl font-semibold mb-6 text-gray-800 dark:text-gray-100">Welcome, Explorer!</h2>
      <p class="mb-8 text-gray-600 dark:text-gray-300">Sign in or create an account to track your travels, wishlist, and notes.</p>
      <div class="flex flex-col gap-4">
        <a href="/login" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg transition">Login</a>
        <a href="/register" class="bg-green-500 hover:bg-green-600 text-white font-bold py-3 rounded-lg transition">Register</a>
      </div>
    <% } %>
  </div>
</body>
</html> 